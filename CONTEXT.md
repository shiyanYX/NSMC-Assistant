# NSMC-Assistant 领域术语

> 川北医学院教务系统（教务系统）的相关术语。本文件是术语表，不涉及实现细节。

## 教务系统

- **教务系统** — 川北医学院的教学管理系统，域名为 `jiaowu3.nsmc.edu.cn`，部署在 `/jsxsd/` 路径下。学生通过学号和密码登录后使用。
- **登录凭证** — 学号（username）和密码（password），通过 Base64 编码后以 `encoded=base64(user)%%%base64(pass)` 格式 POST 到 `xk/LoginToXk`。

## 成绩查询

- **成绩** — 学生在各学期各门课程的最终评分，包含成绩、学分、绩点、考核方式、考试性质、课程属性等字段。
- **学期** — 教务系统中按学年+学期划分的时间段，格式如 `2025-2026-1`（第一学期）、`2025-2026-2`（第二学期）。成绩查询支持按学期筛选。
- **成绩表** — 教务系统页面上 `table#dataList` 中的表格，第一行为表头，其余行为成绩数据行。

## 教学评价（评教）

- **评教** — 学生对本学期每门课程任课教师的教学质量进行评价，是教务系统的强制环节。位于菜单"教学评价 → 教学评价 → 学生评价"下。
- **评教类别** — 评价的分类标签，如"理论课指标"，每门课对应一个评教类别。
- **待评课程列表** — 学生评价页面中，列出本学期所有需要评价的课程和对应教师。每行包含教师编号、教师姓名、所属院系、评教类别、总评分、是否已评、是否提交状态。
- **评价表单** — 对一位教师进行评价的问卷页面，包含 10 道单选题（评价指标）和 1 个建议文本框。每题有五个选项：非常满意、满意、一般、不满意、非常不满意，每个选项对应一个动态权重分值。
- **反满分限制** — 教务系统的客户端校验规则（`isxtjg=1`）：不允许所有题目选择完全相同的选项列（即不能 10 题全选"非常满意"）。需要至少修改一道题的选项来绕过此限制。
- **建议** — 评价表单底部的自由文本输入框，可选填，留空不影响提交。
- **提交与保存** — 评价表单有两个操作按钮：提交（`issubmit=1`，提交后不可修改）和保存（`issubmit=0`，可以修改）。提交操作触发确认对话框后 POST 表单，成功后有"提交成功"提示并返回待评列表。
